/**
 * @deprecated This adapter is replaced by EventBusService in the refactored
 * Event Bus module. This file is kept temporarily for reference only.
 *
 * The new Event Bus uses EventBusService → EventBusPort with register/unregister
 * and DomainEventEnvelope dispatch. See events.module.ts for current wiring.
 */
export {};
